'use client';
// Static recommendations — no AI call needed since we handle related articles server-side
export function Recommendations({ articleContent, currentUrl }: { articleContent: string; currentUrl: string }) {
  return null; // Related articles now handled in main page component
}
